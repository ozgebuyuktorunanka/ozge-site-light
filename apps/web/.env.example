# Optional. Leave empty to use a same-origin /api (recommended with the nginx setup).
# Set to your API origin if the API is hosted on a different domain, e.g.:
# VITE_API_URL=https://api.ozgebuyuktorun.dev
VITE_API_URL=
